1. This script will run for scraping both google and Bing in parallel.
2. There are two input file. First, Text file with all the company names. Second, CSV file with the categorical keywords.
3. Two input file for each google and bing. So, it will take four input files
4. Output will be stored here C:\\outputsCategory\\bing\\ (for bing) and C:\\outputsCategory\\google2\\ (for google2).
   Directory will be created dynamically.